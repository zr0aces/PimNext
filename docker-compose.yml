services:
  printbot:
    build: .
    image: printbot
    container_name: printbot
    restart: always
    devices:
      - /dev/usb/lp0:/dev/usb/lp0
    volumes:
      - /var/run/cups/cups.sock:/var/run/cups/cups.sock
    env_file:
      - .env
